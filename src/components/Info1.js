import { ListItemButton, ListItemText } from "@mui/material";

export default function Info1(props) {

    const { title } = props;
    return (
        <>
            <ListItemButton>
                <ListItemText />
            </ListItemButton>
        </>
    )
}